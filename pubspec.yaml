name: entropy_reducer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.3.8
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.15.7
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
